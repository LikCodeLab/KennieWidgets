<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kennie.example.views">

    <application
        android:name=".AppApplication"
        android:allowBackup="false"
        android:icon="@drawable/ic_logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialComponents.Light.DarkActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 演示勾选(CheckView)  -->

        <activity
            android:name=".ExampleCheckViewActivity"
            android:label="@string/example_check_view" />

        <!-- 演示标签(LabelTagLayout)  -->
        <activity
            android:name=".label.ExampleLabelTagLayoutActivity"
            android:label="@string/example_label_tag_layout_view" />

        <activity android:name=".label.TagShapeActivity" />
        <activity android:name=".label.TagChoiceActivity" />
        <activity android:name=".label.TagEditActivity" />
        <activity android:name=".label.TagChangeActivity" />
        <activity android:name=".label.TagViewActivity" />
        <activity android:name=".label.TagReverseActivity" />

    </application>

</manifest>